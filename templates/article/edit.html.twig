{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    {{ form_end(edit_form) }}
    <p><a href="{{ path('article_index') }}">index</a></p>
{% endblock %}